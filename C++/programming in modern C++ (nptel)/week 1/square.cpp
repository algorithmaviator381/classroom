#include <iostream>
#include <cmath>

using namespace std;

int main(){
    int num;

    cout<<"Input number: ";
    cin>>num;

    double sqrt_num=sqrt(num);

    cout<< "The square root of " << num << " is " << sqrt_num;
}